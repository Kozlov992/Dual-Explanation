# Dual feature-based and example-based explanation methods

This repository contains the code and the model for "Dual feature-based and example-based explanation method" article. 

To install dependencies use pip and requirements.txt:

```python
pip install -r requirements.txt
```
For the brief demonstration view the [example](Example.ipynb).

Each of the experiments featured in the article is connected to its own file. So, to replicate the experiment, just run the associated script.